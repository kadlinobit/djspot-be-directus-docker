{
  "scripts": {
    "dev": "docker compose up -d",
    "snap": "docker compose exec directus npx directus schema snapshot ./snapshots/latest-schema.yaml",
    "apply": "docker compose exec directus npx directus schema apply ./snapshots/latest-schema.yaml"
  }
}
